#pragma once

#include <FEngine/ResourcesManager/CachedResource.h>
#include <map>

namespace feng
{
//    class CachedMaterial final: public CachedResource
//    {
//    public:
//        CachedMaterial() = default;
//        CachedMaterial(const CachedMaterial& other);
//        CachedMaterial(CachedMaterial&& other);
//        ~CachedMaterial() = default;
//
//        CachedMaterial& operator = (const CachedMaterial& other);
//        CachedMaterial& operator = (CachedMaterial&& other);
//
//        unsigned long ShaderId = 0;
//        std::map<const char *, obj_id> Textures;
//        std::map<const char *, float> Floats;
//        std::map<const char *, Vector4> Vec4Values;
//    };
}
